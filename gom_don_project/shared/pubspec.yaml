name: shared
description: Thư viện dùng chung chứa Data Models và Services cho dự án Gom Đơn.
version: 1.0.0
publish_to: 'none' # Không đẩy lên pub.dev, chỉ dùng nội bộ

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Firebase
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  firebase_auth: ^5.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
